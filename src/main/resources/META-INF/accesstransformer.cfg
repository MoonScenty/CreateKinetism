# Stray Chiller: its empty item catches a capturable mob out of a spawner, exactly as Create's
# Empty Blaze Burner does, and that reads the spawner's candidate list. Create opens these same two
# fields in its own access transformer, so at runtime this changes nothing; it is here so this mod
# compiles against them.
public net.minecraft.world.level.BaseSpawner spawnPotentials
public net.minecraft.world.level.BaseSpawner nextSpawnData
